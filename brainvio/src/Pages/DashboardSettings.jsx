import React from 'react';

export default function DashboardSettings() {
    return (
        <div>
            <h2>Settings</h2>
            <p>Account and app settings (notifications, privacy, theme options) will be here.</p>
        </div>
    );
}
